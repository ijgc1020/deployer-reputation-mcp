{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "edges"
  ],
  "properties": {
    "edges": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1000,
      "items": {
        "type": "object"
      },
      "title": "Launch edges",
      "editor": "json",
      "description": "One record per distinct mint; deployer, funder and mint are required. Runtime validates strict field types, limits and Unicode. Sample uses fictional opaque identifiers.",
      "prefill": [
        {
          "deployer": "ExampleDeployer",
          "funder": "ExampleFunder",
          "mint": "ExampleMint",
          "outcome": "unknown",
          "funder_is_cex": false
        }
      ]
    },
    "operation": {
      "title": "Operation",
      "type": "string",
      "enum": [
        "score",
        "cluster"
      ],
      "default": "score",
      "description": "Choose heuristic scoring or association grouping."
    }
  },
  "title": "Supplied launch edges",
  "schemaVersion": 1
}