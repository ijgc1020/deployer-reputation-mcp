{
  "actorOutputSchemaVersion": 1,
  "title": "Supplied-edge heuristic result",
  "properties": {
    "dataset": {
      "type": "string",
      "title": "Result dataset",
      "description": "Authoritative output: one delivered result object per run, with auditable nested cluster counts.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
